rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the leads collection
    match /leads/{document=**} {
      allow read, write: if true;
    }
    // Default rule for all other documents
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
